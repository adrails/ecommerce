<%= form_for(@product_brand) do |f| %>
  <% if @product_brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_brand.errors.count, "error") %> prohibited this product_brand from being saved:</h2>

      <ul>
      <% @product_brand.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brand_name %><br>
    <%= f.text_field :brand_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
